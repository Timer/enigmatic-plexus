# Install javascript engine
Download and install [io.js](https://iojs.org/).

# Install dependencies
To install the libraries, run `npm install`.
Update them periodically with `npm update`.

# Run the code
Execute the program using `iojs kegg.js`.
